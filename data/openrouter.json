{
  "name": "openrouter",
  "display_name": "OpenRouter API",
  "version": "v1",
  "base_url": "https://openrouter.ai/docs",
  "sections": [
    {
      "title": "Quickstart",
      "path": "quickstart",
      "url": "https://openrouter.ai/docs/quickstart",
      "keywords": ["quickstart", "getting started", "installation", "setup", "npm", "sdk", "api key", "openrouter sdk", "first request", "hello world"],
      "use_cases": ["how to get started with OpenRouter", "how to install OpenRouter SDK", "how to make first API request", "how to set up OpenRouter"],
      "tags": ["getting-started", "tutorial"],
      "priority": 10,
      "content": "OpenRouter provides a unified API that gives you access to hundreds of AI models through a single endpoint, while automatically handling fallbacks and selecting the most cost-effective options. Get started with just a few lines of code using your preferred SDK or framework.\n\n## Using the OpenRouter SDK (Beta)\n\nFirst, install the SDK:\n\n```bash\nnpm install @openrouter/sdk\n# or\nyarn add @openrouter/sdk\n# or\npnpm add @openrouter/sdk\n```\n\nThen use it in your code:\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n  defaultHeaders: {\n    'HTTP-Referer': '<YOUR_SITE_URL>', // Optional. Site URL for rankings on openrouter.ai.\n    'X-Title': '<YOUR_SITE_NAME>', // Optional. Site title for rankings on openrouter.ai.\n  },\n});\n\nconst completion = await openRouter.chat.send({\n  model: 'openai/gpt-5.2',\n  messages: [\n    {\n      role: 'user',\n      content: 'What is the meaning of life?',\n    },\n  ],\n  stream: false,\n});\n\nconsole.log(completion.choices[0].message.content);\n```"
    },
    {
      "title": "Using the OpenRouter API Directly",
      "path": "quickstart-api-direct",
      "url": "https://openrouter.ai/docs/quickstart#using-the-openrouter-api-directly",
      "keywords": ["api", "rest api", "fetch", "requests", "curl", "http", "direct api", "python requests", "typescript fetch"],
      "use_cases": ["how to call OpenRouter API directly", "how to use OpenRouter with Python", "how to use curl with OpenRouter", "how to make HTTP requests to OpenRouter"],
      "tags": ["api-reference", "tutorial"],
      "priority": 9,
      "content": "## Using the OpenRouter API directly\n\nYou can use the interactive Request Builder to generate OpenRouter API requests in the language of your choice.\n\n```python\nimport requests\nimport json\n\nresponse = requests.post(\n  url=\"https://openrouter.ai/api/v1/chat/completions\",\n  headers={\n    \"Authorization\": \"Bearer <OPENROUTER_API_KEY>\",\n    \"HTTP-Referer\": \"<YOUR_SITE_URL>\", # Optional\n    \"X-Title\": \"<YOUR_SITE_NAME>\", # Optional\n  },\n  data=json.dumps({\n    \"model\": \"openai/gpt-5.2\",\n    \"messages\": [\n      {\n        \"role\": \"user\",\n        \"content\": \"What is the meaning of life?\"\n      }\n    ]\n  })\n)\n```\n\n```typescript\nfetch('https://openrouter.ai/api/v1/chat/completions', {\n  method: 'POST',\n  headers: {\n    Authorization: 'Bearer <OPENROUTER_API_KEY>',\n    'HTTP-Referer': '<YOUR_SITE_URL>',\n    'X-Title': '<YOUR_SITE_NAME>',\n    'Content-Type': 'application/json',\n  },\n  body: JSON.stringify({\n    model: 'openai/gpt-5.2',\n    messages: [\n      {\n        role: 'user',\n        content: 'What is the meaning of life?',\n      },\n    ],\n  }),\n});\n```\n\n```shell\ncurl https://openrouter.ai/api/v1/chat/completions \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer $OPENROUTER_API_KEY\" \\\n  -d '{\n  \"model\": \"openai/gpt-5.2\",\n  \"messages\": [\n    {\n      \"role\": \"user\",\n      \"content\": \"What is the meaning of life?\"\n    }\n  ]\n}'\n```"
    },
    {
      "title": "Using the OpenAI SDK with OpenRouter",
      "path": "quickstart-openai-sdk",
      "url": "https://openrouter.ai/docs/quickstart#using-the-openai-sdk",
      "keywords": ["openai sdk", "openai compatible", "openai client", "drop-in replacement", "migration", "openai python", "openai typescript"],
      "use_cases": ["how to use OpenAI SDK with OpenRouter", "how to migrate from OpenAI to OpenRouter", "how to use OpenRouter as OpenAI drop-in replacement"],
      "tags": ["tutorial", "migration"],
      "priority": 8,
      "content": "## Using the OpenAI SDK\n\n```typescript\nimport OpenAI from 'openai';\n\nconst openai = new OpenAI({\n  baseURL: 'https://openrouter.ai/api/v1',\n  apiKey: '<OPENROUTER_API_KEY>',\n  defaultHeaders: {\n    'HTTP-Referer': '<YOUR_SITE_URL>',\n    'X-Title': '<YOUR_SITE_NAME>',\n  },\n});\n\nasync function main() {\n  const completion = await openai.chat.completions.create({\n    model: 'openai/gpt-5.2',\n    messages: [\n      {\n        role: 'user',\n        content: 'What is the meaning of life?',\n      },\n    ],\n  });\n\n  console.log(completion.choices[0].message);\n}\n\nmain();\n```\n\n```python\nfrom openai import OpenAI\n\nclient = OpenAI(\n  base_url=\"https://openrouter.ai/api/v1\",\n  api_key=\"<OPENROUTER_API_KEY>\",\n)\n\ncompletion = client.chat.completions.create(\n  extra_headers={\n    \"HTTP-Referer\": \"<YOUR_SITE_URL>\",\n    \"X-Title\": \"<YOUR_SITE_NAME>\",\n  },\n  model=\"openai/gpt-5.2\",\n  messages=[\n    {\n      \"role\": \"user\",\n      \"content\": \"What is the meaning of life?\"\n    }\n  ]\n)\n\nprint(completion.choices[0].message.content)\n```\n\nThe API also supports streaming."
    },
    {
      "title": "OAuth PKCE Authentication",
      "path": "auth-oauth-pkce",
      "url": "https://openrouter.ai/docs/guides/overview/auth/oauth",
      "keywords": ["oauth", "pkce", "authentication", "login", "user auth", "code challenge", "code verifier", "callback url", "api key exchange"],
      "use_cases": ["how to implement OAuth PKCE with OpenRouter", "how to let users login with OpenRouter", "how to get user API key via OAuth", "how to implement one-click login"],
      "tags": ["security", "authentication", "advanced"],
      "priority": 9,
      "content": "Users can connect to OpenRouter in one click using Proof Key for Code Exchange (PKCE).\n\n## PKCE Guide\n\n### Step 1: Send your user to OpenRouter\n\nTo start the PKCE flow, send your user to OpenRouter's `/auth` URL with a `callback_url` parameter:\n\n```\nhttps://openrouter.ai/auth?callback_url=<YOUR_SITE_URL>&code_challenge=<CODE_CHALLENGE>&code_challenge_method=S256\n```\n\nThe `code_challenge` parameter is optional but recommended.\n\n#### How to Generate a Code Challenge\n\n```typescript\nimport { Buffer } from 'buffer';\n\nasync function createSHA256CodeChallenge(input: string) {\n  const encoder = new TextEncoder();\n  const data = encoder.encode(input);\n  const hash = await crypto.subtle.digest('SHA-256', data);\n  return Buffer.from(hash).toString('base64url');\n}\n\nconst codeVerifier = 'your-random-string';\nconst generatedCodeChallenge = await createSHA256CodeChallenge(codeVerifier);\n```\n\n### Step 2: Exchange the code for a user-controlled API key\n\nAfter the user logs in, they are redirected back with a `code` parameter. Exchange it:\n\n```typescript\nconst response = await fetch('https://openrouter.ai/api/v1/auth/keys', {\n  method: 'POST',\n  headers: {\n    'Content-Type': 'application/json',\n  },\n  body: JSON.stringify({\n    code: '<CODE_FROM_QUERY_PARAM>',\n    code_verifier: '<CODE_VERIFIER>',\n    code_challenge_method: '<CODE_CHALLENGE_METHOD>',\n  }),\n});\n\nconst { key } = await response.json();\n```\n\n### Step 3: Use the API key\n\nStore the API key securely and use it to make OpenRouter requests.\n\n## Error Codes\n\n- `400 Invalid code_challenge_method`: Use the same method in step 1 and step 2.\n- `403 Invalid code or code_verifier`: Ensure user is logged in and verifier is correct.\n- `405 Method Not Allowed`: Use `POST` and `HTTPS`."
    },
    {
      "title": "Provisioning API Keys",
      "path": "auth-provisioning-keys",
      "url": "https://openrouter.ai/docs/guides/overview/auth/provisioning-api-keys",
      "keywords": ["provisioning", "api keys", "key management", "create key", "delete key", "key rotation", "saas", "programmatic keys"],
      "use_cases": ["how to create API keys programmatically", "how to manage API keys for SaaS application", "how to rotate API keys", "how to set usage limits on API keys"],
      "tags": ["api-reference", "security", "advanced"],
      "priority": 8,
      "content": "OpenRouter provides endpoints to programmatically manage your API keys, enabling key creation and management for applications that need to distribute or rotate keys automatically.\n\n## Creating a Provisioning API Key\n\n1. Go to the Provisioning API Keys page\n2. Click \"Create New Key\"\n3. Complete the key creation process\n\nProvisioning keys cannot be used to make API calls to OpenRouter's completion endpoints - they are exclusively for key management operations.\n\n## Use Cases\n\n- **SaaS Applications**: Automatically create unique API keys for each customer instance\n- **Key Rotation**: Regularly rotate API keys for security compliance\n- **Usage Monitoring**: Track key usage and automatically disable keys that exceed limits\n\n## Example Usage\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: 'your-provisioning-key',\n});\n\n// List API keys\nconst keys = await openRouter.apiKeys.list();\n\n// Paginate\nconst keysPage2 = await openRouter.apiKeys.list({ offset: 100 });\n\n// Create a new API key\nconst newKey = await openRouter.apiKeys.create({\n  name: 'Customer Instance Key',\n  limit: 1000, // Optional credit limit\n});\n\n// Get a specific key\nconst key = await openRouter.apiKeys.get(keyHash);\n\n// Update a key\nconst updatedKey = await openRouter.apiKeys.update(keyHash, {\n  name: 'Updated Key Name',\n  disabled: true,\n  limitReset: 'daily', // Reset limit every day at midnight UTC\n});\n\n// Delete a key\nawait openRouter.apiKeys.delete(keyHash);\n```\n\n## Response Format\n\n```json\n{\n  \"data\": [\n    {\n      \"created_at\": \"2025-02-19T20:52:27.363244+00:00\",\n      \"hash\": \"<YOUR_KEY_HASH>\",\n      \"label\": \"sk-or-v1-abc...123\",\n      \"name\": \"Customer Key\",\n      \"disabled\": false,\n      \"limit\": 10,\n      \"limit_remaining\": 10,\n      \"limit_reset\": null,\n      \"usage\": 0\n    }\n  ]\n}\n```"
    },
    {
      "title": "BYOK - Bring Your Own Keys",
      "path": "auth-byok",
      "url": "https://openrouter.ai/docs/guides/overview/auth/byok",
      "keywords": ["byok", "bring your own key", "provider keys", "custom keys", "azure", "bedrock", "vertex", "rate limits", "fallback"],
      "use_cases": ["how to use own provider API keys with OpenRouter", "how to configure BYOK", "how to set up Azure keys with OpenRouter", "how to use AWS Bedrock with OpenRouter"],
      "tags": ["advanced", "security", "providers"],
      "priority": 7,
      "content": "## Bring your own API Keys\n\nOpenRouter supports both OpenRouter credits and the option to bring your own provider keys (BYOK).\n\nUsing provider keys enables direct control over rate limits and costs via your provider account. Your provider keys are securely encrypted.\n\nManage keys in your account settings.\n\nThe cost of using custom provider keys on OpenRouter is a percentage of what the same model/provider would cost normally on OpenRouter and will be deducted from your OpenRouter credits. This fee is waived for the first requests per-month.\n\n### Key Priority and Fallback\n\nOpenRouter always prioritizes using your provider keys when available. By default, if your key encounters a rate limit or failure, OpenRouter will fall back to using shared OpenRouter credits.\n\nYou can configure individual keys with \"Always use this key\" to prevent any fallback.\n\n### BYOK with Provider Ordering\n\nWhen you combine BYOK keys with provider ordering, OpenRouter tries all your BYOK endpoints first (in the specified order), then falls back to OpenRouter's shared capacity.\n\n```json\n{\n  \"provider\": {\n    \"allow_fallbacks\": true,\n    \"order\": [\"Amazon Bedrock\", \"Google Vertex AI\", \"Anthropic\"]\n  }\n}\n```\n\n### Azure API Keys\n\nTo use Azure AI Services with OpenRouter:\n\n```json\n{\n  \"model_slug\": \"openai/gpt-5.2\",\n  \"endpoint_url\": \"https://example-project.openai.azure.com/openai/deployments/gpt-5.2/chat/completions?api-version=2024-08-01-preview\",\n  \"api_key\": \"your-azure-api-key\",\n  \"model_id\": \"gpt-5.2\"\n}\n```\n\n### AWS Bedrock API Keys\n\nOption 1: Bedrock API Keys (Recommended)\n```\nyour-bedrock-api-key-here\n```\n\nOption 2: AWS Credentials\n```json\n{\n  \"accessKeyId\": \"your-aws-access-key-id\",\n  \"secretAccessKey\": \"your-aws-secret-access-key\",\n  \"region\": \"your-aws-region\"\n}\n```\n\n### Google Vertex API Keys\n\nProvide your Google Cloud service account key in JSON format with an optional `region` field.\n\n### Debugging BYOK Issues\n\nView provider responses on the Activity page. Look for the `provider_responses` field which shows HTTP status codes.\n\nCommon BYOK Error Codes:\n- 400 Bad Request: Invalid request format\n- 401 Unauthorized: Invalid or revoked API key\n- 403 Forbidden: Missing permissions\n- 429 Too Many Requests: Rate limit hit"
    },
    {
      "title": "Models API",
      "path": "models",
      "url": "https://openrouter.ai/docs/guides/overview/models",
      "keywords": ["models", "models api", "list models", "model schema", "pricing", "context length", "supported parameters", "architecture"],
      "use_cases": ["how to list available models", "how to get model pricing", "how to check model context length", "how to find model capabilities"],
      "tags": ["api-reference", "models"],
      "priority": 8,
      "content": "Explore and browse 400+ models and providers on our website, or with our API. You can also subscribe to our RSS feed to stay updated on new models.\n\n## Models API Standard\n\nOur Models API makes the most important information about all LLMs freely available.\n\n### API Response Schema\n\n```json\n{\n  \"data\": [\n    /* Array of Model objects */\n  ]\n}\n```\n\n### Model Object Schema\n\n| Field | Type | Description |\n| --- | --- | --- |\n| `id` | `string` | Unique model identifier (e.g., `\"google/gemini-2.5-pro-preview\"`) |\n| `canonical_slug` | `string` | Permanent slug that never changes |\n| `name` | `string` | Human-readable display name |\n| `created` | `number` | Unix timestamp when added |\n| `description` | `string` | Model capabilities description |\n| `context_length` | `number` | Maximum context window in tokens |\n| `architecture` | `Architecture` | Technical capabilities |\n| `pricing` | `Pricing` | Price structure |\n| `top_provider` | `TopProvider` | Primary provider config |\n| `supported_parameters` | `string[]` | Supported API parameters |\n\n### Pricing Object\n\nAll pricing values are in USD per token/request/unit.\n\n```typescript\n{\n  \"prompt\": string,           // Cost per input token\n  \"completion\": string,       // Cost per output token\n  \"request\": string,          // Fixed cost per API request\n  \"image\": string,            // Cost per image input\n  \"web_search\": string,       // Cost per web search\n  \"internal_reasoning\": string,\n  \"input_cache_read\": string,\n  \"input_cache_write\": string\n}\n```\n\n### Supported Parameters\n\n- `tools` - Function calling capabilities\n- `tool_choice` - Tool selection control\n- `max_tokens` - Response length limiting\n- `temperature` - Randomness control\n- `top_p` - Nucleus sampling\n- `reasoning` - Internal reasoning mode\n- `structured_outputs` - JSON schema enforcement\n- `response_format` - Output format specification\n- `stop` - Custom stop sequences\n- `seed` - Deterministic outputs"
    },
    {
      "title": "Multimodal Overview",
      "path": "multimodal-overview",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/overview",
      "keywords": ["multimodal", "images", "pdf", "audio", "video", "vision", "file upload", "base64", "input modalities"],
      "use_cases": ["how to send images to OpenRouter", "how to process PDFs with OpenRouter", "how to use audio with OpenRouter", "how to send video to OpenRouter"],
      "tags": ["multimodal", "tutorial"],
      "priority": 8,
      "content": "OpenRouter supports multiple input modalities beyond text, allowing you to send images, PDFs, audio, and video files to compatible models through our unified API.\n\n## Supported Modalities\n\n### Images\nSend images to vision-capable models for analysis, description, OCR, and more.\n\n### Image Generation\nGenerate images from text prompts using AI models with image output capabilities.\n\n### PDFs\nProcess PDF documents with any model on OpenRouter. Our intelligent PDF parsing system extracts text and handles both text-based and scanned documents.\n\n### Audio\nSend audio files to speech-capable models for transcription, analysis, and processing.\n\n### Video\nSend video files to video-capable models for analysis, description, object detection, and action recognition.\n\n## Getting Started\n\nAll multimodal inputs use the same `/api/v1/chat/completions` endpoint:\n\n- **Images**: Use `image_url` content type\n- **PDFs**: Use `file` content type with PDF data\n- **Audio**: Use `input_audio` content type\n- **Video**: Use `video_url` content type\n\n## Input Format Support\n\n### URLs (Recommended for public content)\n- **Images**: `https://example.com/image.jpg`\n- **PDFs**: `https://example.com/document.pdf`\n- **Audio**: Not supported via URL (base64 only)\n- **Video**: Provider-specific\n\n### Base64 Encoding (Required for local files)\n- **Images**: `data:image/jpeg;base64,{base64_data}`\n- **PDFs**: `data:application/pdf;base64,{base64_data}`\n- **Audio**: Raw base64 string with format specification\n- **Video**: `data:video/mp4;base64,{base64_data}`"
    },
    {
      "title": "Image Inputs",
      "path": "multimodal-images",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/images",
      "keywords": ["images", "vision", "image url", "base64 image", "image analysis", "ocr", "image description", "multimodal"],
      "use_cases": ["how to send images to OpenRouter", "how to analyze images with AI", "how to use base64 images", "how to do OCR with OpenRouter"],
      "tags": ["multimodal", "tutorial"],
      "priority": 7,
      "content": "Requests with images, to multimodel models, are available via the `/api/v1/chat/completions` API with a multi-part `messages` parameter.\n\n### Using Image URLs\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n});\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-3-flash-preview',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        {\n          type: 'text',\n          text: \"What's in this image?\",\n        },\n        {\n          type: 'image_url',\n          imageUrl: {\n            url: 'https://example.com/image.jpg',\n          },\n        },\n      ],\n    },\n  ],\n  stream: false,\n});\n```\n\n### Using Base64 Encoded Images\n\n```typescript\nasync function encodeImageToBase64(imagePath: string): Promise<string> {\n  const imageBuffer = await fs.promises.readFile(imagePath);\n  const base64Image = imageBuffer.toString('base64');\n  return `data:image/jpeg;base64,${base64Image}`;\n}\n\nconst base64Image = await encodeImageToBase64('path/to/image.jpg');\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-3-flash-preview',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: \"What's in this image?\" },\n        { type: 'image_url', imageUrl: { url: base64Image } },\n      ],\n    },\n  ],\n});\n```\n\nSupported image content types:\n- `image/png`\n- `image/jpeg`\n- `image/webp`\n- `image/gif`"
    },
    {
      "title": "Image Generation",
      "path": "multimodal-image-generation",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/image-generation",
      "keywords": ["image generation", "generate images", "text to image", "ai art", "flux", "gemini image", "modalities", "aspect ratio"],
      "use_cases": ["how to generate images with OpenRouter", "how to create AI art", "how to set image aspect ratio", "how to use text-to-image models"],
      "tags": ["multimodal", "advanced"],
      "priority": 7,
      "content": "OpenRouter supports image generation through models that have `\"image\"` in their `output_modalities`.\n\n## API Usage\n\nTo generate images, send a request with the `modalities` parameter set to include both `\"image\"` and `\"text\"`.\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n});\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-2.5-flash-image-preview',\n  messages: [\n    {\n      role: 'user',\n      content: 'Generate a beautiful sunset over mountains',\n    },\n  ],\n  modalities: ['image', 'text'],\n  stream: false,\n});\n\nif (result.choices) {\n  const message = result.choices[0].message;\n  if (message.images) {\n    message.images.forEach((image, index) => {\n      const imageUrl = image.imageUrl.url; // Base64 data URL\n      console.log(`Generated image ${index + 1}`);\n    });\n  }\n}\n```\n\n### Image Configuration Options\n\n#### Aspect Ratio\n\nSupported aspect ratios:\n- `1:1` → 1024×1024 (default)\n- `2:3` → 832×1248\n- `3:2` → 1248×832\n- `16:9` → 1344×768\n- `9:16` → 768×1344\n\n#### Image Size (Gemini only)\n\n- `1K` → Standard resolution (default)\n- `2K` → Higher resolution\n- `4K` → Highest resolution\n\n```typescript\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-3-pro-image-preview',\n  messages: [{ role: 'user', content: 'Create an image' }],\n  modalities: ['image', 'text'],\n  image_config: {\n    aspect_ratio: '16:9',\n    image_size: '4K',\n  },\n});\n```\n\n## Response Format\n\nImages are returned as base64-encoded data URLs in the `images` field."
    },
    {
      "title": "PDF Inputs",
      "path": "multimodal-pdfs",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/pdfs",
      "keywords": ["pdf", "document processing", "pdf parsing", "ocr", "mistral ocr", "file upload", "pdf text extraction"],
      "use_cases": ["how to process PDFs with OpenRouter", "how to extract text from PDFs", "how to use OCR with OpenRouter", "how to analyze documents"],
      "tags": ["multimodal", "tutorial"],
      "priority": 7,
      "content": "OpenRouter supports PDF processing through the `/api/v1/chat/completions` API. PDFs can be sent as direct URLs or base64-encoded data URLs.\n\n## PDF Processing Engines\n\n1. `mistral-ocr`: Best for scanned documents or PDFs with images (paid)\n2. `pdf-text`: Best for well-structured PDFs with clear text content (Free)\n3. `native`: Only available for models that support file input natively\n\n## Using PDF URLs\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n});\n\nconst result = await openRouter.chat.send({\n  model: 'anthropic/claude-sonnet-4',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: 'What are the main points in this document?' },\n        {\n          type: 'file',\n          file: {\n            filename: 'document.pdf',\n            fileData: 'https://bitcoin.org/bitcoin.pdf',\n          },\n        },\n      ],\n    },\n  ],\n  plugins: [\n    {\n      id: 'file-parser',\n      pdf: { engine: 'mistral-ocr' },\n    },\n  ],\n  stream: false,\n});\n```\n\n## Using Base64 Encoded PDFs\n\n```typescript\nasync function encodePDFToBase64(pdfPath: string): Promise<string> {\n  const pdfBuffer = await fs.promises.readFile(pdfPath);\n  const base64PDF = pdfBuffer.toString('base64');\n  return `data:application/pdf;base64,${base64PDF}`;\n}\n\nconst base64PDF = await encodePDFToBase64('path/to/document.pdf');\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemma-3-27b-it',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: 'What are the main points?' },\n        {\n          type: 'file',\n          file: { filename: 'document.pdf', file_data: base64PDF },\n        },\n      ],\n    },\n  ],\n});\n```\n\n## Skip Parsing Costs\n\nWhen you send a PDF, the response may include file annotations. By sending these annotations back in subsequent requests, you can avoid re-parsing the same PDF."
    },
    {
      "title": "Audio Inputs",
      "path": "multimodal-audio",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/audio",
      "keywords": ["audio", "speech", "transcription", "audio input", "wav", "mp3", "audio processing", "speech to text"],
      "use_cases": ["how to transcribe audio with OpenRouter", "how to send audio files to AI", "how to process speech with OpenRouter"],
      "tags": ["multimodal", "tutorial"],
      "priority": 6,
      "content": "OpenRouter supports sending audio files to compatible models via the API.\n\n**Note**: Audio files must be base64-encoded - direct URLs are not supported for audio content.\n\n## Sending Audio Files\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\nimport fs from 'fs/promises';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n});\n\nasync function encodeAudioToBase64(audioPath: string): Promise<string> {\n  const audioBuffer = await fs.readFile(audioPath);\n  return audioBuffer.toString('base64');\n}\n\nconst audioPath = 'path/to/your/audio.wav';\nconst base64Audio = await encodeAudioToBase64(audioPath);\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-2.5-flash',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: 'Please transcribe this audio file.' },\n        {\n          type: 'input_audio',\n          inputAudio: {\n            data: base64Audio,\n            format: 'wav',\n          },\n        },\n      ],\n    },\n  ],\n  stream: false,\n});\n```\n\n## Supported Audio Formats\n\n- `wav` - WAV audio\n- `mp3` - MP3 audio\n- `aiff` - AIFF audio\n- `aac` - AAC audio\n- `ogg` - OGG Vorbis audio\n- `flac` - FLAC audio\n- `m4a` - M4A audio\n- `pcm16` - PCM16 audio\n- `pcm24` - PCM24 audio\n\n**Note:** Check your model's documentation to confirm which audio formats it supports."
    },
    {
      "title": "Video Inputs",
      "path": "multimodal-video",
      "url": "https://openrouter.ai/docs/guides/overview/multimodal/videos",
      "keywords": ["video", "video input", "video analysis", "youtube", "video url", "video processing", "mp4", "video understanding"],
      "use_cases": ["how to analyze video with OpenRouter", "how to send video to AI", "how to use YouTube videos with OpenRouter", "how to do video understanding"],
      "tags": ["multimodal", "advanced"],
      "priority": 6,
      "content": "OpenRouter supports sending video files to compatible models via the API.\n\n**Important:** Video URL support varies by provider. Google Gemini on AI Studio only supports YouTube links.\n\n## Using Video URLs\n\n```typescript\nimport { OpenRouter } from '@openrouter/sdk';\n\nconst openRouter = new OpenRouter({\n  apiKey: '<OPENROUTER_API_KEY>',\n});\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-2.5-flash',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: 'Please describe what is happening in this video.' },\n        {\n          type: 'video_url',\n          videoUrl: { url: 'https://www.youtube.com/watch?v=dQw4w9WgXcQ' },\n        },\n      ],\n    },\n  ],\n  stream: false,\n});\n```\n\n## Using Base64 Encoded Videos\n\n```typescript\nasync function encodeVideoToBase64(videoPath: string): Promise<string> {\n  const videoBuffer = await fs.promises.readFile(videoPath);\n  const base64Video = videoBuffer.toString('base64');\n  return `data:video/mp4;base64,${base64Video}`;\n}\n\nconst videoPath = 'path/to/your/video.mp4';\nconst base64Video = await encodeVideoToBase64(videoPath);\n\nconst result = await openRouter.chat.send({\n  model: 'google/gemini-2.5-flash',\n  messages: [\n    {\n      role: 'user',\n      content: [\n        { type: 'text', text: \"What's in this video?\" },\n        { type: 'video_url', videoUrl: { url: base64Video } },\n      ],\n    },\n  ],\n});\n```\n\n## Supported Video Formats\n\n- `video/mp4`\n- `video/mpeg`\n- `video/mov`\n- `video/webm`\n\n## Common Use Cases\n\n- Video Summarization\n- Object and Activity Recognition\n- Scene Understanding\n- Sports Analysis\n- Educational Content Analysis"
    },
    {
      "title": "Principles",
      "path": "principles",
      "url": "https://openrouter.ai/docs/guides/overview/principles",
      "keywords": ["principles", "why openrouter", "benefits", "pricing", "availability", "rate limits", "unified api", "multi-model"],
      "use_cases": ["why use OpenRouter", "what are the benefits of OpenRouter", "how does OpenRouter pricing work", "what makes OpenRouter different"],
      "tags": ["getting-started", "overview"],
      "priority": 6,
      "content": "OpenRouter helps developers source and optimize AI usage. We believe the future is multi-model and multi-provider.\n\n## Why OpenRouter?\n\n**Price and Performance**. OpenRouter scouts for the best prices, the lowest latencies, and the highest throughput across dozens of providers, and lets you choose how to prioritize them.\n\n**Standardized API**. No need to change code when switching between models or providers. You can even let your users choose and pay for their own.\n\n**Real-World Insights**. Be the first to take advantage of new models. See real-world data of how often models are used for different purposes. Keep up to date in our Discord channel.\n\n**Consolidated Billing**. Simple and transparent billing, regardless of how many providers you use.\n\n**Higher Availability**. Fallback providers, and automatic, smart routing means your requests still work even when providers go down.\n\n**Higher Rate Limits**. OpenRouter works directly with providers to provide better rate limits and more throughput."
    }
  ]
}
